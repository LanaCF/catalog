const doc = document;

const formRadio = doc.forms.formRadio;
const videoCamera = doc.querySelector('.videocamera');
const microphones = doc.querySelector('.microphones');

const formBrand = doc.forms.formBrand;
const brand = formBrand.brand;

const forModel = doc.forms.forModel;
const model = forModel.model;

const btnSend = doc.querySelector('.send');

let selectedBrand = null;

// const preview = doc.querySelector('.preview');

